
function dataProvider_openweathermap(cityName) {

    this.getData = function(funcToHandleData) {

        obj = {
            name: "Berlin",
            weather: "Rainy",
            temperature: 12,
            errorMsg: ""
        };
    };
}

module.exports = dataProvider_openweathermap;